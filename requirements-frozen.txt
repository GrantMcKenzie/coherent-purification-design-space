# Exact environment used to produce every number and figure.
# Verified on Python 3.12.3 (x86_64) and Python 3.11 (macOS arm64).
# Regenerate with: pip freeze > requirements-frozen.txt
numpy==2.4.4
scipy==1.17.1
sympy==1.14.0
matplotlib==3.10.8
pytest==9.1.1
